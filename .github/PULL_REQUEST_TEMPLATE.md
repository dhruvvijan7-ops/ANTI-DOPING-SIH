## Summary

<!-- What does this PR do, in one or two sentences? -->

## Why

<!-- Which requirement (§N), stage, or decision (D-00N) does this address? What problem does it solve? -->

## Changes

<!-- Bullet list of the meaningful changes. -->

## Testing

<!-- How was this verified? List the commands you ran (e.g. `cd backend && pytest`) and the results. -->

- [ ] Backend tests pass (`pytest`)
- [ ] No secrets introduced (no `.env`, keys, tokens)
- [ ] No generated files committed (`__pycache__`, `.venv`, `node_modules`, `dist`, coverage)

## Documentation

<!-- Which docs were updated to stay in sync (README, ARCHITECTURE, DATABASE, PROJECT_STATUS, TRACEABILITY, DECISIONS)? "None" is only acceptable if nothing changed that docs describe. -->

## Product boundary check

- [ ] No "guilt probability" / "doping score" / automatic "violation confirmed" constructs introduced (D-004)

## Screenshots (if UI change)

<!-- Optional. -->